language: generic 
sudo: required
services: 
  - docker

before_install: 
  - docker build -t irynastein/docker_project1 Dockerfile.dev .

  script: 
    - docker run -e CI=true irynastein/docker_project1 npm run test -- --coverage